package pe.com.zzynan.procardapp.ui.theme

import androidx.compose.ui.graphics.Color

// File: app/src/main/java/pe/com/zzynan/procardapp/ui/theme/Color.kt

// Paleta principal inspirada en Mr. Olympia.
val OlympiaRed = Color(0xFFD7261E) // Rojo dominante para acciones primarias y énfasis.
val OlympiaBlack = Color(0xFF111111) // Negro profundo para textos principales en tema claro.
val IronGray = Color(0xFF3A3A3A) // Gris para textos secundarios en tema claro.
val SteelSilver = Color(0xFFC5C5C5) // Plata para textos secundarios en tema oscuro.
val PureWhite = Color(0xFFFFFFFF) // Blanco puro para texto en superficies oscuras.

// Colores de fondo y superficies según tema.
val DarkBackground = Color(0xFF0D0D0D) // Fondo dominante en tema oscuro.
val DarkCard = Color(0xFF1A1A1A) // Superficie elevada en tema oscuro, usada en cards y app bar.
val DarkPrimaryLight = Color(0xFFFF4A3F) // Resaltado para íconos o elementos interactivos en tema oscuro.
val DarkPrimaryDark = Color(0xFFA1120D) // Variante más profunda para contenedores en tema oscuro.

val LightBackground = Color(0xFFFFFFFF) // Fondo base del tema claro.
val LightSurface = Color(0xFFF4F4F4) // Superficies claras como tarjetas y app bar.
val LightPrimaryLight = Color(0xFFFF6A5F) // Variante brillante para contenedores en tema claro.
val LightPrimaryDark = Color(0xFFA1120D) // Tono intenso para enfatizar botones en tema claro.
val LightSecondaryText = Color(0xFF555555) // Texto secundario en tema claro.
val LightBorder = Color(0xFFE0E0E0) // Bordes sutiles en tema claro.
val DarkBorder = Color(0xFF2A2A2A) // Bordes sutiles en tema oscuro.

// Colores extendidos para futuros módulos (gráficos, insignias y métricas).
val Platinum = Color(0xFFE5E5E5)
val TitanGray = Color(0xFF7A7A7A)
val Charcoal = Color(0xFF262626)
val IronBlue = Color(0xFF1A2A3F)
val VictoryGold = Color(0xFFD4AF37) // Dorado para logros y distintivos Pro Card.
